1. Explain the difference between a singly linked list and a doubly linked list.

A singly linked list stores only one link called next, which points to the next node. A doubly linked list stores two links, next and prev, so nodes can move forward and backward.

2. Observe the Node class, which contains the attributes next and prev. What are the purposes of these attributes?

The next attribute stores the address of the next node, while the prev attribute stores the address of the previous node. These attributes connect nodes together in a doubly linked list.

3. Examine the constructor in the DoubleLinkedLists class. What is the purpose of this constructor?

DoubleLinkedLists(){
head = null;
tail = null;
}

The constructor initializes the doubly linked list by setting head and tail to null. This indicates that the linked list is empty when the object is created.

4. In the addFirst() method, what is the meaning of the following code?

if(isEmpty()){
head = tail = newNode;
}

This code checks whether the linked list is empty. If it is empty, the new node becomes both the head and the tail because it is the only node in the list.

5. In the addFirst() method, what does the statement head.prev = newNode mean?

The statement head.prev = newNode connects the old head node back to the new node. It updates the prev link of the old head.

6. In the insertAfter() method, what is the meaning of current.next.prev = newNode?

The statement current.next.prev = newNode updates the previous link of the next node so that it points back to the newly inserted node.

7. In the experiment 1 code, in which method is the traversal process implemented? What is the meaning of temp = temp.next?

The traversal process is implemented in methods that use loops such as while(temp != null). The statement temp = temp.next moves the pointer to the next node during traversal.

8. In the insertAfter() method, what is the following code for?

if(temp == tail){
addLast(data);
}

Do we have to implement it? What if we remove it?

This code checks whether the current node is the tail node. If it is the tail, the new data is inserted using addLast(data).

Yes, it should be implemented. If it is removed, inserting after the tail node may cause errors such as NullPointerException because the tail does not have a next node.

9. In the insertAfter() what is this statement if(temp.data.nim.equalsIgnoreCase(key)) for?

This statement checks whether the current node’s nim matches the given key value. It is used to find the correct node where the new node should be inserted after it.

10. In the above output, after inserting data after 111 and 112, an Insertion failed. Data (111) not found!! warning is displayed. It should be no warning message, since key 111 is found in the list. Explain the reason why it happens? And correct the code!

The warning appears because after the loop finishes, temp becomes null, so the condition if(temp == null) always becomes true even when the key is found.

To fix this, use a boolean variable to track whether the key is found.
public void print(){
        if (!isEmpty()){
            Node currNode = head;

            while (currNode != null){
                currNode.data.print();
                currNode = currNode.next;
            }
            System.out.println("");

        }else{
            System.out.println("the Linked list is currently empty");
        }
    }
