Experiment 1 Questions 

1.	In the base case of the Divide and Conquer algorithm for calculating factorial, explain the differences
 in the code structure between the if and else conditions!

 Answer :
            this conditional statement is the stoping point for this recursive function it says if x==1 then return 1 beacuse 1! is equal 1 
            so if in the begining user enter 1 program will return 1 other case it works x-1 so if user enter 5 it will start 5-1 then 4-1 then 3-1 and 
            so on when x reach 1 it  will stop the function from recalling beacuse the condition comes true.
             

2.	Is it possible to modify the loop in the factorialBF() method to use an alternative 
to the for loop? Please explain and give example if needed!

Answer: yes it is possible using while 
             int fac = 1;
        int i=1;
        while(i<=x){
            fac = fac*i;
            i++;
        }

        return fac;
    updated int the program as well.


3.	Please explain the differences between facto = facto*i; and int facto = n * factorialDC(n-1); !

    Answer:
            facto = facto*i;  this is using brute force method for-loop or while loop
            int intiial facto is ste to 1 facto=1; i=1; when the loop increase the facto will multiplied with 
            i example 3! 

            so facto =1;  i=1;

            loop increase i=2 so 2*facto which is one so facto = 2

            now loop increase i=3  so 3*facto which is 2 so facto = 6

            now facto = 6 and 3! is 6 

            ////////
            now int facto = n * factorialDC(n-1);
            this is recursive function  
            it will multiply the orignaal value of n with function n-1

            example : 4!

            n=4 

            facto = n*factorialDC(n-1);
            return facto;

            factorialDC(4)
            = 4 * factorialDC(3)
            = 4 * (3 * factorialDC(2))
            = 4 * (3 * (2 * factorialDC(1)))
            = 4 * (3 * (2 * (1 * factorialDC(0))))
            = 4 * 3 * 2 * 1 * 1
            = 24



Experiment 2 Questions 

1.	Explain the differences between the two methods created powerBF() dan powerDC()!

Answer : 

        powerBf() yhis is int type of method using for loop to solve the the exponention problem using brute force method.
        
        powerDC() this method follow the divide and conqurer rules and solve the the exponention problem this is also type int 
        which return value.

2.	Does the combine stage exist in the provided code? Show the relevant part!

Answer : 
        yes combine stage is present the 
        if (exponent%2==1){
                return (powerDc(base, exponent/2) * powerDc(base, exponent/2)*base);
            }else 
            {
                return (powerDc(base, exponent/2)*powerDc(base, exponent/2));
            }


3.	In the powerBF() method, parameters are used to pass the base number and its exponent, even though the Power class 
        already contains attributes for these values (baseNumber and exponent atribute). Do you think it is still relevant
         for the method to have parameters? Could the method be implemented without parameters instead? If so, how would the
          powerBF() method be structured without parameters?

Answer : 
        not strictly but if you want flixability then parameters are necessery  if you dont use parameters the code will still work 
        but not flixable 
        
        int powerBf(){

            int result =1;
            for (int i=0; i<exponent; i++){
                result *=base;
            }
            return result;
        }

4.	Summarize how the powerBF() and powerDC() methods work!

Answer :
        powerBf() is brute forece method using the trick of checiking every possiblity to solve the problem like using loops and more things 

        powerDc() is method like recursive method using diviide and conqurer rules to solve the problem;


Experiment 3


1.	Why is mid variable needed in totalDC()method?

Answer : 
        the mid variable is used to find the the mid value using int mid = (l+r)/2;


2.	Explain the following statements in totalDC()method?
double leftSum = totalDc(profit, left, mid);
 double rightSum = totalDc(profit, mid+1, right);

 Answer :
        this first line is used to find the sum of the left elements in array propit and voiceversa.


3.	Why is it necessary to sum the results of lsum and rsum as shown below??
    return leftSum+rightSum;

Answer :
        it is necessery beacuse we have to combine the results from both sides to get the actual results;


4.	What is the base case of totalDC()method?

Answer :
        the base case for this method is 
        if left == right then return prfit[left];





